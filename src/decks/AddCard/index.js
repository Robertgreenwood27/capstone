import React from 'react';

const AddCard = () => {
    return (
        <div>
            <h1>Allows the user to add a new card to an existing deck</h1>
        </div>
    )
}

export default AddCard;